<h2><%= @task.title %></h2>

<p><%= @task.details %></p>

<div>
<input type="checkbox" id="" <%= @task.completed == true ? "checked" : "" %>>
<label for=""><%= @task.completed == true ? "This task is completed" : "This task is not completed" %></label>
</div>

<div class="div"><%= link_to "Edit", edit_task_path(@task.id) %></div>

<p><%= link_to "Back to list", tasks_path%></p>
